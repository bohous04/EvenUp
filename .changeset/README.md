# Changesets

This folder holds [Changesets](https://github.com/changesets/changesets). Add one
for any user-facing change:

```bash
pnpm changeset
```

Pick the affected packages and a semver bump, and write a short summary. On
release, changesets are consumed to version packages and update the changelog.
