packages:
  - 'apps/*'
  - 'packages/*'

# Native build scripts we explicitly trust (pnpm blocks postinstall scripts by default).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  sharp: true
  unrs-resolver: true
